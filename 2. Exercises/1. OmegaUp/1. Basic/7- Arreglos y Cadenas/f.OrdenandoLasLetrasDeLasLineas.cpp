#include <iostream>
#include <string>
#include <cctype>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    string t, res; 
    getline(cin, t);

    for (char c: t){  
        if (!isalpha(c)) {

        }
    }   
    cout << t; 

    return 0;
}