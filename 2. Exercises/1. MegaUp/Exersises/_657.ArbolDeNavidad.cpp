#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n; cin>>n;
    int lenght = (n*2) +1;

    for (int i=0; i<n+2; ++i) {
        if (i>=n) cout << "##";

        else {
            for (int j=0; j<lengj)
        }

    }

    
    return 0;
}