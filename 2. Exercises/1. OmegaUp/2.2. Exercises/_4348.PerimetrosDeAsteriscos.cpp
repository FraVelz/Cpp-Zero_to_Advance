#include <iostream>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int h,w; cin>>h>>w;

    if (h==1 && w==1) {cout<<'*'; return 0;}

    for (int i=0; i<h; +i)

    return 0;
}