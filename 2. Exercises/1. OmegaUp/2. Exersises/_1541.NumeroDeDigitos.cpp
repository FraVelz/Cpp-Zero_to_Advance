#include <iostream>
#include <string>

using namespace std;

int factorial(int a) {
    if (a <= 1) return a;

    return a * factorial(a-1);
}
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    string n; cin >> n;

    int digit = n.size();
    int res = 
    cout<<;
    
    return 0;
}