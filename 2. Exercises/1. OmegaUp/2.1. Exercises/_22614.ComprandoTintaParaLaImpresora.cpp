#include <iostream>

using namespace std;

struct tinta {
    int cuesta;
    int rinde;
};

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    tinta liquida, seca;
    int hojas, envio, minimoGratis;

    cin >> liquida.rinde >> liquida.cuesta;
    cin >> seca.rinde    >> seca.cuesta;

    cin >> envio >> minimoGratis >> hojas;




    return 0;
}
